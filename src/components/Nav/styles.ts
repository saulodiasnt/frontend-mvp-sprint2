import { styled } from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 3rem;
  z-index: 1100;
  position: relative;

  .logo img {
    width: 130px;
    cursor: pointer;
  }
`;
